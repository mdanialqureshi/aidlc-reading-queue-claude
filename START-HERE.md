# Start Here - Claude Code

## Verify

```bash
cd ~/workspace/ai-engineering/aidlc-reading-queue-claude
aidlc doctor
claude
```

If Claude asks you to approve project hooks, approve the AI-DLC hooks and
restart Claude Code as instructed.

Inside Claude Code:

```text
/aidlc --doctor
```

## Start the workflow

Shorter learning run:

```text
/aidlc express Read ./vision.md and build what it describes.
```

Fuller lifecycle:

```text
/aidlc feature Read ./vision.md and build what it describes.
```

Use **Guide Me** during your first run and review each artifact before
approving it.

